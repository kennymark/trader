{
  "name": "trader",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build -w @trader/shared && npx convex dev & npm run dev -w @trader/web",
    "dev:web": "npm run dev -w @trader/web",
    "dev:convex": "npx convex dev",
    "build": "npm run build -w @trader/shared && npm run build -w @trader/web",
    "test": "vitest run",
    "deploy": "npx convex deploy --cmd 'npm run build' --cmd-url-env-var-name VITE_CONVEX_URL"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@convex-dev/better-auth": "^0.12.5",
    "better-auth": ">=1.6.11 <1.7.0",
    "convex": "^1.45.0",
    "resend": "^6.26.0",
    "yahoo-finance2": "^4.0.2"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "vitest": "^3.2.7"
  }
}
